{
  "name": "root",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm ci --prefix server && cd client && npm ci && npm run start:prod",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": "19.8.1"
  }
}
