{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd client && npm start",
    "start": "cd server && npm install && npm start"
  },
  "devDependencies": {
    "@types/node": "^18.16.1"
  }
}
