{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "devDependencies": {
    "@types/node": "^18.16.1"
  }
}
