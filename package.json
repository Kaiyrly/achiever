{
  "name": "root",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm ci --prefix server && cd client && npm ci",
    "start": "cd server && node server.js && cd ../client && npm start"
  },
  "engines": {
    "node": "19.8.1"
  }
}
