{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npm start",
    "heroku-postbuild": "cd server && npm install && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
