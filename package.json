{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build && cd .. && serve -s client/build",
    "start": "cd server && node server.js"
  },
  "devDependencies": {
    "@types/node": "^18.16.1"
  }
}
